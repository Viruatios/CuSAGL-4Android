# 简易自动化原琴：安卓版 - CuSAGL-4Android / CuSAGL Mobile

[CuSimpAutoGenshinLyre](https://github.com/Viruatios/CuSimpAutoGenshinLyre) 的安卓移植版。

---

## 使用说明

### 适用场景

本应用用于在 Android 设备上演奏《原神》“风物之诗琴”等同类点按弹奏琴界面。

### 首次准备

1. 确认您的设备符合以下要求：
    - Android 12 及以上版本。
    - 已安装《原神》或《云·原神》游戏。
    - 游戏内能够稳定保持至少 30 FPS 的帧率，并且能够正常进入原琴界面。
2. 安装并打开应用。
3. 按首页提示授予“悬浮窗权限”和“无障碍服务权限”：
   - 悬浮窗权限用于显示播放控制面板。
   - 无障碍服务用于向游戏琴键位置发送触控手势。
   - （可选）网络访问权限用于检查并下载更新。
   - （可选）安装未知应用权限用于应用内安装更新版本 APK。

### 基本流程

1. 在“曲谱管理”中导入 `.json` 曲谱，或使用手动创建入口新建曲谱。
2. 在“播放配置”中选择播放模式、单曲或队列、循环次数、曲目间隔和循环间隔等选项。
3. 回到首页，按当前配置预加载曲谱。预加载会把曲谱解析为播放时间轴缓存，避免正式演奏时临时计算造成卡顿。
4. 当曲谱缓存、悬浮窗权限和无障碍服务都就绪后，点击“准备演奏”。
5. 切换到游戏琴界面，通过悬浮窗控制播放：
   - “开始/暂停”用于控制当前曲目的播放状态。
   - “停止”会结束本次播放会话并关闭悬浮窗。
   - “上一首/下一首”用于在已预加载队列中切换曲目。

悬浮窗在空闲、暂停或停止状态下可以拖动；正在播放时会锁定位置，避免误触影响演奏。

***重要：演奏进行期间，请务必保持游戏处于琴界面！如果要退出原琴界面，请首先“暂停”或“退出”演奏！***

### 曲谱和缓存存储

- 导入后的曲谱保存在应用私有目录 `filesDir/score_file`，文件名会规范化为 `####.曲名.json` 的形式。
- 预加载缓存保存在应用私有目录 `filesDir/cache`。曲谱被覆盖或删除时，对应缓存会同步清理。
- 播放配置保存在应用私有目录 `filesDir/playback_config.json`。

---

## 开发与构建配置

- 使用仓库内 Gradle Wrapper 构建：Windows 下运行 `gradlew.bat`，其他平台运行 `./gradlew`。
- 当前 Android Gradle Plugin 为 `9.2.1`，Kotlin Compose 插件为 `2.2.10`，Compose BOM 为 `2026.05.01`。
- 本地需要配置 Android SDK；SDK 路径由本机 `local.properties` 提供。
- 推荐验证命令：`gradlew.bat :app:testDebugUnitTest :app:assembleDebug`。

---

## 曲谱编写说明

Android 版沿用原 JS 脚本的键盘谱格式，并在导入和手动创建时做严格校验。曲谱文件必须是 UTF-8 编码的 JSON。

### JSON 基本结构

```json
{
  "name": "示例曲名",
  "author": "录谱人",
  "instrument": "风物之诗琴",
  "description": "曲谱说明",
  "type": "keyboard",
  "bpm": 120,
  "time_signature": "4/4",
  "composer": "作曲者",
  "arranger": "编曲者",
  "notes": "Q W / E R\n(AD) S / D F"
}
```

### 字段说明

- `name`：必要字段，曲谱名，不能为空。
- `bpm`：必要字段，正整数，表示每分钟节拍数。
- `time_signature`：必要字段，格式为 `N/D`，例如 `4/4`、`3/4`、`6/8`；分母必须是 2 的幂。
- `notes`：必要字段，键盘谱正文，不能为空，且必须能解析出至少一个有效音符或休止单位。
- `type`：建议保留为 `keyboard`，表示键盘谱格式。
- `author`、`instrument`、`description`、`composer`、`arranger`：可选字段，用于展示曲谱信息；缺省时应用会使用默认文本。

### 键位范围

曲谱使用 21 键键盘谱字母，对应游戏琴键三行：

```text
Q W E R T Y U
A S D F G H J
Z X C V B N M
```

小写字母会按大写处理。括号、空格和 `/` 之外的无效内容不会产生有效按键；编写时建议只使用上述键位和规则符号，避免误读。

### 读谱规则

1. `notes` 中每一行（两个 `\n` 之间）视作一个小节。
2. `/` 是一拍的停止符；一行内可以用多个 `/` 分隔多个拍。当前解析器会把 `/` 两侧都计入小节拍数，因此行尾保留 `/` 会产生一个尾部空拍；不需要尾部空拍时，可以省略最后一个 `/`。
3. 空格是基本单位的停止符；同一拍内以空格分隔的基本单位会均分这一拍的时长。
4. 连续两个空格会产生一个休止单位；行首空格也可表示该拍开头的休止。
5. 单个字母表示单键，例如 `Q`。
6. 括号内的多个字母表示和弦，同时按下，例如 `(QW)`。
7. 不带空格连续书写的多个音块表示琶音/连续按键，它们会在该基本单位的时长内继续细分并依次演奏。例如 `QWE` 会演奏为 `Q W E`，`(QW)E(RT)` 会演奏为 `(QW) E (RT)`。
8. 每个基本单位必须等上一个基本单位结束后才进入下一个基本单位；运行时还会保留最小抬键间隔，减少重复按键被游戏吞掉的情况。

### 节拍和时值

- `bpm` 决定数拍的基础速度。
- `time_signature` 的分母决定一拍基准；例如 `4/4` 以四分音符为一拍，`6/8` 会按复合拍规则处理。
- 每个 `/` 分隔出的拍内，所有基本单位平均分配该拍时长。

示例：

```text
Q W E R
```

在 `4/4`、`bpm = 120` 时，这一拍被分成 4 个基本单位，`Q`、`W`、`E`、`R` 依次演奏。

```text
Q W / E R
```

这一行包含两拍：第一拍内 `Q`、`W` 各占半拍，第二拍内 `E`、`R` 各占半拍。

```text
Q  W
```

`Q` 和 `W` 中间有两个空格，表示 `Q` 后存在一个休止单位；这一拍会被分成 `Q`、休止、`W` 三个基本单位。

```text
(QW)E(RT)
```

这是一个连续按键基本单位，内部包含和弦 `(QW)`、单键 `E` 和和弦 `(RT)`，会在这一基本单位的时长内继续等分演奏。

### 最小可用示例

```json
{
  "name": "小星星片段",
  "type": "keyboard",
  "bpm": 120,
  "time_signature": "4/4",
  "notes": "Q Q / G G\nH H / G"
}
```

---

## 第三方内容的引用说明

- 应用的图标来自《原神》内的“风物之诗琴”，由 miHoYo 设计，版权归 miHoYo 所有。该图标仅用于本项目的演示和测试目的，不用于任何商业用途。
- 项目由 CuSimpAutoGenshinLyre 移植并改写而来。原项目是运行在 BetterGI 上的 JS 脚本，开发参考了 @提瓦特钓鱼玳师 和 @半江残秋 老师的 AutoYuanQin。代码移植完成后，原 JS 脚本项目从本项目中移除。

---

# 开发记录

_以下是动工前的规划：_

原本想的是在安卓上实现一个类似于 BetterGI 的 JS 脚本宿主环境，提供脚本需要调用的接口，从而可以复用已有的 JS 处理脚本。后来觉得脚本中某些功能，比如预解析曲谱，完全不必在演奏前那点时间、顶着开了游戏的高负载去做，它可以在一个更灵活的时间来执行。所以脚本里不同功能的代码本身就有拆分并融入到项目中的合理性，这种情况下仍然将脚本视作一个整体去执行是不妥当的。此外，数据类型转换、异步回调、内存管理和异常捕获上这些方面处理也比较麻烦，脚本代码其实并不如预期般容易复用。遂推翻，改为将脚本翻译为 Kotlin 原生实现。

## 预期功能

1. 申请无障碍服务（点击操作）和悬浮窗权限（实时控制）。应用每次启动时需要检查权限是否已授予，若无，则弹窗提示，并引导用户前往设置界面授予权限。此外，应用需要确保自身能够在后台保持运行。
2. 应用的主界面使用 Jetpack Compose 构建。应当具有以下功能：
   - 曲谱管理入口。允许用户导入或删除 JSON 曲谱；或提供工具给用户，协助他们按规定的格式自行创建曲谱 JSON 文件。
   - 曲谱演奏的自定义配置选项入口。提供包括“选定曲谱”、“选定曲谱列表”、“单曲单次演奏”、“单曲循环演奏”、“列表循环演奏”等选项。
   - 提供原 JS 脚本中的“预加载曲谱并保存缓存文件”功能。这个任务现在被单独拆分了出来，在设备负载更低的时候完成。
   - “准备演奏”按钮。只允许在上述自定义配置选项完整，且曲谱预加载已完成时启用。点击后，进入“悬浮窗模式”。
3. 在“悬浮窗模式”下，应用应当在屏幕上显示一个 (可拖动的) 悬浮窗，包含以下功能：
   - 显示当前选定的曲谱名称和演奏状态（如“正在演奏”、“已暂停”等）。
   - 提供“开始/暂停”按钮，允许用户控制演奏的开始和暂停。
   - 提供“停止”按钮，允许用户立即停止演奏并退出悬浮窗模式。
   - 提供“上一首/下一首”按钮，允许用户在已经预加载的曲谱列表中切换当前演奏的曲谱。
   - (可选) 在悬浮窗模式下，持续监测游戏状态（如是否在游戏内、是否在演奏界面等），并根据游戏状态自动调整演奏行为（例如，检测到当前游戏不处于演奏界面时，自动暂停演奏动作）。
4. 触屏模式下，将键盘谱转换为点击屏幕上对应点位的功能，详见 [触屏模式下的键盘谱演奏应该如何确认按键位置](#附录触屏模式下的键盘谱演奏应该如何确认按键位置)。

## 脚本移植的具体计划

等待被移植的 JS 脚本暂时存放于 `OriginScripts\CuSimpAutoGenshinLyre` 文件夹下，它将不会被打包进入最终程序中。

_以下内容部分参考了 Copilot 建议。_

> 直接将 JS 逻辑人工/半自动翻译为 Kotlin 原生代码。

不考虑引入 C++ 或 Rust 来重写业务逻辑，对于零 Android 经验的开发者来说，那会是一场灾难。Kotlin 本身是一门非常现代的语言，它的很多语法特性与现代 JavaScript / TypeScript 惊人地相似。

以下是具体的方案和高效起步的步骤：

### 核心方案：JS 到 Kotlin 的逐行映射

Kotlin 和 JS 具有极高的语法亲和力，核心任务是完成**思维模式的转换**。性能上，直接跑在 Android ART 虚拟机上的 Kotlin 代码，可以胜过任何桥接形式的 JS 引擎，完全满足项目对性能的严苛要求。

---

### 高效落地步骤（Step-by-Step）

为了追求开发效率，按照以下步骤对原有 JS 脚本进行移植：

#### 步骤 1：梳理数据模型（JSON -> Data Class）

JS 脚本通常重度依赖 JSON 数据流转。在强类型的 Kotlin 中，第一步是把这些数据结构固定下来。

- _示例_：
  ```kotlin
  // JS: const user = { id: 1, name: "Alice" }
  data class User(val id: Int, val name: String)
  val user = User(1, "Alice")
  ```

#### 步骤 2：剥离纯逻辑函数，使用 AI 辅助翻译

把 JS 脚本中那些**不涉及 DOM、不涉及网络请求**的纯计算、纯数据处理函数（Pure Functions）提取出来。 因为这是纯逻辑转换，**直接把大段的 JS 代码发给 Copilot**，可以瞬间帮你转化成地道、高性能的 Kotlin 代码。

#### 步骤 3：改造异步请求（Promise -> Coroutines）

JS 脚本里有 `fetch` 或者异步的文件读取，在 Kotlin 中则需要用**协程**来处理。 在 Kotlin 中，将异步函数标记为 `suspend`。

- _示例_：
  ```javascript
  // JS 异步
  async function fetchData(url) {
  	let res = await fetch(url);
  	return await res.json();
  }
  ```
  ```kotlin
  // Kotlin 协程 (通常配合 Retrofit 或 OkHttp 等网络库)
  suspend fun fetchData(url: String): MyData {
      // 挂起函数，不会阻塞主线程
      val response = httpClient.get(url).await()
      return parseJson(response)
  }
  ```

#### 步骤 4：单元测试验证（兜底保障）

由于没有 Android 开发经验，如果把逻辑直接塞进 UI 里跑，一旦出错，你很难分辨是 UI 写错了还是逻辑写错了。

- 把翻译好的 Kotlin 逻辑写在 `src/test/java/...` 目录下，写几个简单的单元测试（JUnit）。
- 将 JS 脚本的输入输出作为测试用例，喂给你的 Kotlin 函数，断言结果一致即可。这样可以脱离 Android 模拟器，在几毫秒内验证逻辑的正确性。

### 将 UI 开发和逻辑迁移分开。

1. 先建一个纯 Kotlin 的文件（不接触任何 Android UI 元素），利用 AI 工具配合，把你的 JS 算法、处理逻辑完整翻译成 Kotlin 函数和类。
2. 写个简单的 `main` 函数或测试用例跑通。
3. 最后再用 Jetpack Compose 把这些原生函数绑定到按钮和列表中。

## 附录：触屏模式下的键盘谱演奏应该如何确认按键位置

### 《原神》针对不同分辨率进行“风物之诗琴”缩放坐标变换公式

基于观察得到的原神的 UI 缩放策略推断，对于任意目标分辨率，坐标映射公式如下：

1. **基准设定**: 基准分辨率为 `W_base = 1920`, `H_base = 1080`。
2. **计算缩放比 (Scale)**:
   为了防止 UI 过小，《原神》采用了宽和高中变化更大的比例作为整体防裁剪缩放比：
   `Scale = max(W_target / W_base, H_target / H_base)`
3. **坐标变换**:
   由于“风物之诗琴”属于底部 UI，排版规则为“X 轴居中，Y 轴底部对齐”。
   设基准点坐标为 `(x_base, y_base)`，目标点坐标为 `(x_target, y_target)`：
   - **X轴转换 (居中对齐)**:
     `x_target = x_base * Scale + (W_target - W_base * Scale) / 2`
   - **Y轴转换 (底部对齐)**:
     `y_target = y_base * Scale + (H_target - H_base * Scale)`

在编写坐标映射代码时，可将预先在 1920x1080 下采集好的琴键中心坐标 `(x_base, y_base)` 代入上述公式，即可适配所有非常规长宽比的 Android 设备屏幕。

### 1920x1080 分辨率下的基准 键盘-触屏 琴键坐标表

```kotlin
private val BASE_COORDINATES = mapOf(
    'Q' to PointF(455f, 670f),
    'W' to PointF(625f, 670f),
    'E' to PointF(790f, 670f),
    'R' to PointF(960f, 670f),
    'T' to PointF(1125f, 670f),
    'Y' to PointF(1295f, 670f),
    'U' to PointF(1460f, 670f),
    'A' to PointF(455f, 805f),
    'S' to PointF(625f, 805f),
    'D' to PointF(790f, 805f),
    'F' to PointF(960f, 805f),
    'G' to PointF(1125f, 805f),
    'H' to PointF(1295f, 805f),
    'J' to PointF(1460f, 805f),
    'Z' to PointF(455f, 940f),
    'X' to PointF(625f, 940f),
    'C' to PointF(790f, 940f),
    'V' to PointF(960f, 940f),
    'B' to PointF(1125f, 940f),
    'N' to PointF(1295f, 940f),
    'M' to PointF(1460f, 940f)
)
```

---

_以下是_

## 原 JS 脚本的开发和更新记录

_考虑到本项目核心逻辑由原 JS 脚本移植和改写而来，因此在此记录原 JS 脚本的开发和更新历史，以便更好地理解当前版本的设计决策和演变过程。_

### v0.1.0 修改后的读谱规则

1. 只对于“点按弹奏琴”有效，只对于“键盘谱”有效。不支持其他类型。
2. 定义：每个音符为一个基本单位，它可能是“休止符”、“单个按键”、“同时按下按键”或是“连续按键”。
3. 定义：“空格”和“/”都是“停止符”，表示位于该符号前面的音符演奏到这里停止。其中 “空格” 是 “基本单位的停止符”，而 “/” 是 “一拍的停止符”。
4. 定义：单行之中的按键视作一小节，即不同小节之间以“\n”分割。
5. 规定：一拍的时长由BPM决定；每一拍内的“基本单位”均分这一拍的时长。
6. 规定：对于每一拍内，只有上一个“基本单位”的持续时长结束，才能演奏下一个“基本单位”。该等待规则不仅作用于每一拍之间，也作用于每个“基本单位”之间。
7. 规定：对于每个“基本单位”，单个字母视作“单个按键”，一个“()”内的所有字母视作“同时按下按键”，多个字母则视作“连续按键”。
8. 约定：“基本单位”也可以是一个“休止符”，“休止符”在谱序列中不占位置。例如，谱序列“空格+A+空格+B+空格+空格+/”，被解读为“休止符+音符A+音符B+休止符+这一拍结束”。
9. 约定：在“/”之前不允许具有“休止符”，因此“休止符”只能存在于“空格停止符”之前。此外，由于每小节基本以拍为单位，因此实际操作上忽略“\n”的停止符作用。
10. 约定：对于“连续按键”内的多个字母按键，每个字母按键均分该“连续按键”的时长。
11. 约定：“连续按键”是一个特殊的“基本单位”，其中也允许存在“同时按下按键”和“单个按键”的混合，演奏时按照它们各自的演奏规则进行。例如“(AB)C(DE)”是一个“连续按键”，其中包含了一个“同时按下按键”(AB)，一个“单个按键”C和另一个“同时按下按键”(DE)。

### v0.1.1 对播放调度模型的修改

原模型为何不符合演奏先后规则：

1. 对每个音符调用一次异步 notePlay，但不等待它结束。这样“下一个基本单位必须等上一个基本单位结束”只靠 offset 间接实现，不是强约束。只要计时抖动或调度顺序变化，就可能出现时值不符合预期。
2. 两个协程都在操作同一个键。例如前一单位末尾 keyup 与下一单位开头 keydown 在同一时间点附近触发时，若前一单位的 keyup 晚到，会把下一单位刚按下的键立即抬起，导致后一个单位实际时长变短。
3. wait 和 time 都做 Math.floor，单位越细（例如一拍内分得越多），累计截断误差越明显。结果是“单位结束时间”会早于理论值，听感上就是持续时长不准。
4. 结论：核心的问题在播放调度模型：当前是“按音符并发定时”，不是“按基本单位串行执行”。这会直接破坏“上一个基本单位结束后再进入下一个”的强等待规则。解析侧（offset、unitDuration）大体是对的，但会被并发竞态和取整误差放大。

改动：

1. 序列化结构升级为“基本单位序列”

- `keySheetSerialization` 现在输出：`[小节拍数, ...基本单位]`。
- 基本单位结构为：`{ kind: rest|single|chord|arpeggio, keys: string[], time: number }`。
- 这使播放层可以直接按单位串行执行，不再依赖“按音符并发 + offset”间接对齐。

2. 播放调度模型重构为单位时间轴串行驱动

- 每个基本单位执行前必须等待到其开始时刻，执行完成后才进入下一个单位。
- 调度使用 `await` 串行推进，从根上保证“上一个单位结束后再进入下一个单位”。
- 同键位的 `keyup/keydown` 竞态被消除，避免单位边界误抬键导致时值缩短。

3. 时值对齐策略调整

- 改为基于绝对时间点对齐（`sleepUntil`）并使用 `Math.round`，降低累积截断误差。
- 每小节结束会对齐到理论小节终点，减少长曲播放漂移。

4. 行为结果

- 串行、和弦、琶音与休止符都在同一时间轴模型下执行，时值一致性更高。

### v0.1.2 读谱细节优化

- 如果一拍的内容编写为 "A B /"（注意 B 后面有一个用于排版的空格），按 / 首先被分割为 "A B "（末尾多一个空格）。
  当对 "A B " 执行 .split(' ') 时，会被分割为 ["A", "B", ""]。此时 units.length 变成了 3（而不是预期的 2），多出来的 "" 触发了定义的“两个空格之间视为休止符”的逻辑。这导致原本各占 1/2 拍的 A 和 B，变成了各占 1/3拍，并在结尾多出了 1/3拍的休止符。
- 通过将每一拍的字符串在切割前进行 .trim()（去除头尾排版多余空格）解决了该问题。不仅去除了该副作用，而且仍然完美支持内部连续空格（例如 "A B" 中间连续两个空格）产生休止符的逻辑。

### v0.1.3 按键细节优化

- 当出现两个相同的连续基本按键（例如 A 后面紧跟着还是 A）时：

  第一拍结尾处，代码执行 keyUp('A') 抬起按键。
  JS 是单线程的，代码在一两毫秒内就迅速走完了对下一拍按键时间的计算并回到这里，直接执行下一拍的 keyDown('A')。
  两次操作之间的真实物理时间几乎为0（小于1-2毫秒）。
  原神等相当一部分游戏的引擎，是通过循环轮询（如以60Hz，即大约每16毫秒轮询一次）来检测输入状态的。在这样一次微小得连一帧都不到的过程中，按键状态经历了“按下 -> 抬起 -> 再次按下”。对于引擎来说，因为状态变化太快，它根本没捕捉到“抬起”这个动作，也就不会产生第二次“按下”的独立事件，最终导致相同的后续按键就像“被跳过了”一样，俗称**“吞键”**。

- 解决：在每一个演奏方法中添加了自动“提前抬键以切断连音”的机制：

  通过强制让每个音符在结束前留出约5毫秒的物理抬键空隙，就可以保证游戏引擎能切实地识别到前一个键已经“弹起”，进而完美触发后续重复的“基本单位”这个问题。这一修改也完全生效于和弦与连续按键中。

### v0.1.4 增加并落实连续按键的解析规则

- 改进了底层按键的解析方式（正则分割）：
  弃用了之前过于简单而容易导致逻辑冲突的 startsWith 判断，改用正则表达式 /\([A-Za-z]+\)|[A-Za-z]/g 将形如 (AB)C(DE) 的复杂基本单位精确切分成不同的原子块 —— 即 ["(AB)", "C", "(DE)"]。
  这样该特殊的连续按键会被存储成一个两级数组的形式 [['A','B'], ['C'], ['D','E']] 用于后续播放。

- 改进了执行器 unitPlay 中的遍历触发器：
  在 if (unit.kind === "arpeggio") 分支的循环调度中，兼容了这种嵌套数组写法（对原单纯的单音琶音也能自动向后兼容为数组）。由于同一块内可能存在多个待触发的按键，我在计算好每一等分的 StartTime 之后，将直接使用 for (const key of keyGroup) 同时 keyDown 块内的所有按键（如同时触发 A 和 B），等待单块时长结束后，再同时调用 keyUp 抬起所有按键，最后再衔接进下一个等分块中进行同样的操作。

现在，像如 (AB)C(DE) 之类的复杂排版单位将可以严密地先均分总时长，并在各自被分配的一等分时间内按上述规则准确演奏。

### v0.1.5 分隔符改用“-”

- 将规则中的分割符从“空格”修改为“-”，以避免与排版空格产生冲突。这是因为在之前的版本中，使用空格作为分隔符导致了诸如 "A B /" 这类情况的解析错误（末尾多一个空格导致多出一个空字符串，从而引入了意外的休止符）。改用“-”后，用户可以自由使用空格进行排版美化，而不会影响实际的音符解析和时值计算。

### v0.1.6 对读谱规则进行重大修改，重定义部分概念，撤销v0.1.2和v0.1.5（重要）

- v0.1.0中将“空格”和“/”错误地定义为“属于两段基本单位之间的分割”，导致了诸如 " A B /" 这类情况的解析错误（末尾多一个空格导致多出一个空字符串，从而引入了意外的休止符；v0.1.2又导致了开头的休止符无法被识别）。因此在v0.1.6中对读谱规则进行了重新定义和细化，明确了“空格”和“/”的作用和限制，以避免类似问题的发生。同时撤销了v0.1.2和v0.1.5中对分隔符和空格处理的修改，回归到更为简洁和直观的读谱方式。
- 新定义：
    1. “空格”和“/”都是“停止符”，表示位于该符号前面的音符演奏到这里停止。其中 “空格” 是 “基本单位的停止符”，而 “/” 是 “一拍的停止符”。
    2. “基本单位”也可以是一个“休止符”，“休止符”在谱序列中不占位置。
    3. 约定：在“/”之前不允许具有“休止符”，因此“休止符”只能存在于“空格停止符”之前。例如，谱序列“ A B /”，被解读为“休止符+音符A+休止符+音符B+这一拍结束”。

### v0.1.7 非四分音符节拍支持

- 在音乐中，BPM（Beats Per Minute） 大多默认以 四分音符（1/4 拍） 作为一拍的时间基准的。当谱子是 4/4 拍时，分母为 4（即四分音符），那么每拍的时长就是 60000 / BPM 毫秒，当前代码在这个基础上运算完美。但是，当遇到 6/8 拍的乐谱时，它的分母为 8（基础节拍是八分音符）。此时一个 / 之间表示的是一个八分音符，如果在代码中仍然强行把它当作四分音符计算时长（60000 / BPM），它的时间实际上只给了一半，这就会导致实际运行起来仿佛上了两倍速一样，产生异常加快。
- 通过加入对于 time_signature 拍号分母因子的自适应解析，当遇到例如 4/8 等非四分音符基底的拍号时，将会自动对 gap（每一拍的时长）乘以 (分母 / 4)（即对于 4/8 源分母 8，将 gap 时间翻倍 8/4 = 2）以修正速度过快的问题。

#### v0.1.7b 复合拍子（如 6/8 拍）节拍支持修正

- 修复了 v0.1.7 中处理 `6/8` 等拍号时演奏速度异常变慢的问题。在音乐理论中，对于分子能被 3 整除的八分拍（如 `6/8`、`9/8` 等“复合拍子”），其基本物理节拍单位并非单纯的“八分音符”或“两个四分音符”，而是**附点四分音符**（即 1.5 个四分音符）。
- 之前的版本中将 6/8 拍的一拍强行拉长到了两个四分音符的时值（乘以 `8/4 = 2.0` 倍数），导致播放实际速度慢了约 33%。
- 新的修改重新加入了复合拍子的判定分支：当遇到分母为 8 且分子为 3 的倍数的复合拍号（如 `6/8`）时，将正确赋予它们 `1.5` 倍四分音符的时值乘数。其他常规拍号则按真实的数学比例正常缩放，确保曲谱的 BPM 最终在各个类型的拍号里都能以完美的预期原速进行演奏。

### v0.1.8 演奏功能不生效修复

- 修复了 JS 脚本在 `settings.json` 中部分配置“看起来可填、实际不生效”的问题。

- 问题根因：
    1. 播放主循环没有严格区分“单次模式”和“循环模式”，导致单次模式也可能被循环次数参数影响。
    2. 队列配置解析不够严格：队列为空时没有正确按“默认全部播放”处理，序号匹配也存在误匹配风险。
    3. 定时启动与间隔参数缺少统一的容错与默认值策略，部分输入下会出现行为与配置不一致。

- 本次改动：
    1. 重构设置解析逻辑：统一解析并校验 `music_start`、`music_repeat`、`music_interval`、`repeat_interval`。
    2. 明确四种播放模式行为：
        - 单曲单次执行：只执行 1 轮。
        - 单曲循环：按循环次数执行，`music_repeat=0` 时无限循环。
        - 队列单次执行：队列仅执行 1 轮。
        - 队列循环：队列按循环次数执行，`music_repeat=0` 时无限循环。
    3. 修复队列规则：
        - `music_queue` 留空时，默认按本地曲谱顺序播放全部曲目。
        - 队列序号改为按四位前缀精确匹配（如 `1` 对应 `0001.*`），避免模糊匹配造成选曲错误。
    4. 修正间隔生效范围：
        - `music_interval` 仅在队列模式下生效，且只在“曲目与曲目之间”等待。
        - `repeat_interval` 仅在循环模式下、且下一轮确实会执行时等待。

- 修复结果：
  `settings.json` 中以下配置现已按预期生效：定时播放、单曲循环、演奏队列单次执行、演奏队列循环、队列内演奏间隔时间。

### v0.1.9 演奏按键操作优化

- 添加物理状态机 keyState：在 listNotePlay 的顶部增加了一个 keyState 对象字典，记录每一个具体按钮的最新 Last_KeyUp_Time。
- 定义最小保护间隙 MIN_GAP_TIME = 25：在调用任意下压键动作前，校验当前时间距上一次此键被松开时间的差值，如果低于 MIN_GAP_TIME 强制进行 sleepUntil 阻塞等待（甚至越级短暂停顿），以牺牲微小宏观精度换取引擎 100% 捕获键位状态改变。
    - 我认为，25ms 的间隔已经是一个足够短的时间，除非有什么谱子需要一秒按20次同一个键，那个谱子也就不是这个脚本能干的了。
- 独立的按键执行发射器 emitKeyPulse(key, targetHalfTime)：将繁琐的 keyDown(key) 后跟随计算时间的逻辑完全抽离封装了起来。
- 动态调节与脉冲式发音：原逻辑是占满音符的绝大部分时长执行按压，现已改为执行一个计算出的简短按下过程 const holdTime = Math.min(MIN_GAP_TIME, targetHalfTime) 配合短暂延时后直接抬起。这相当于我们只用脉冲向游戏传达信号，剩余所有的 await sleepUntil(unitEndTime) 区间都是安全的物理抬起状态（空窗期最大化）。
- 并发触发和弦：利用 Promise.all 并发处理多按键（Chord、Arpeggio 分组），使得和弦音更加同时触发且各自走自己独立的防重叠校验逻辑。

#### v0.1.9b 支持后台演奏

- 在 listNotePlay 函数内创建了一个后台消息实例 postMessage = new PostMessage()，并将原先直接调用的 keyDown(key) 和 keyUp(key) 替换为 postMessage.keyDown(key) 和 postMessage.keyUp(key)，以支持通过后台消息的方式来触发按键事件。
- 现在游戏内演奏不必总是保持游戏前台了。

### v0.1.10 性能优化

- 高精度时间源：引入了容错包装的 const getNow = () => typeof performance !== 'undefined' ? performance.now() : Date.now(); 自动调用受支持的最高精度时间打点源。如支持，则使用 performance.now() 提供亚毫秒级的时间精度，显著降低时间计算误差，提升长时间演奏的节拍稳定性。
- 逻辑全预存计算（AOT）：将以往在弹奏时实时计算的“延时等待、防防吞音补偿、动态时差”全部用模拟时钟 currentSimTime 前置到了弹奏前的预处理阶段。通过计算推演生成一个巨大的原始打点 timeline 数组，从而保证弹奏时不再产生动态的状态计算开销。
- 事件的时间序列化与无伤规避：对生成的离散 timeline 数组进行多重规则重排（先抓时间向先后顺序；遇到同时到达的目标，保证 Up（抬起）永远先由于 Down（按下）被触发）。
- 事件扁平化与聚合：将时间点完全相同且动作行为一致的方法，例如某个和弦引发的多个瞬间 keyDown 操作合并在一起，原本会同时生成4个sleep对象，现在扁平化为了一个对象：[[{ time: 150, action: "down", keys: ["W", "E"] }]]。
- 单必定时间轴扫描播放器：移除了所有的 async function sleepUntil、unitPlay 等拆分状态机，运行时只剩单纯的一个微任务循环去扫描合并时间事件序列数组。现在只需挂起一次 await sleep 然后直接并发派发所有按键指令，真正实现了毫无时间差计算和异步协程切分损耗的纯粹性能级回放。

#### v0.1.10b 引入缓存机制与其他优化

- 引入预处理时间轴保存为缓存文件的机制，资源路径更新：新增了常数 cache_path 指向 cache 文件夹。
- 预处理逻辑解耦，运行时播放器重构：将原来紧耦合在 listNotePlay 函数中的时间轴编译过程抽取为独立的 prebakeTimeline 计算函数；将剩余部分提炼为纯粹只负责通过延时播放解析完毕数组流的 playCachedTimeline 运行时函数。
- 缓存文件生成机制：对于没有缓存的曲目，代码会在通过 prebakeTimeline 获取 mergedTimeline 和 totalCalculatedTime 等信息后，构造一个包含曲目详细描述、小节数、批次指令数目、总时长、 create_time 和乐曲的单拍时长 gap 的对象，并转化为 JSON，根据曲名保存在缓存路径中。后续再遇该曲可以直接快速加载。
- 缓存的过期判断和清理机制：新增了 checkAndCleanCache、getFileModTime 等工具函数，在 main 程序入口启动时首先调用。若缓存对应的曲谱丢失，或者通过 System.IO.File 获取到的曲谱修改时间比缓存的修改时间更新，则认为这部分缓存数据已过期，自动清理。
- 其他：实现了高频正则操作的去重与原生替换优化，并调整了 get_settings() 和 checkSheetFile() 让它们共用同一个来自 musicList() 的文件目录数组参数，省去原来重复且昂贵的磁盘 I/O。

#### v0.1.10c 修复了新代码中，键盘谱解析时跳过纯休止符拍的问题

#### v0.1.10d 播放器引入混合休眠策略，引入懒加载，与其他优化

- 缓存检查降级为元数据 IO（解决启动卡顿峰值）：修改了 checkAndCleanCache 函数，不再读取整个 .json 缓存文件的文本并进行 JSON.parse 昂贵的序列化操作，现在直接调用 getFileModTime(entry) 获取缓存文件的最后修改时间，和原曲谱的修改时间 modTime 进行低成本比对，若缓存修改更早就直接判定过期。

- 播放器引入 Hybrid Sleep / Spin-Wait 混合休眠策略（解决计时精准度）：在 playCachedTimeline 核心调度循环中，改用 remain > 5 的阈值判断，即当距离目标触发时间差大于 5 毫秒时才会出让 CPU 使用 await sleep(remain - 5) 休眠。剩余时间全部转入 while (getNow() < targetTime) 进行自旋式对齐。消除传统 sleep() 带来的 JavaScript 定时器系统导致的数毫秒误差与积压延迟。

- 在播放循环引入懒加载(Lazy Load)，重构生命周期（解决内存 OOM 溢出风险）：脚本启动时 music_infos 数组现在只填充诸如 { music_name: "0001.xxx" } 此类基础轻量对象，不会预加载庞大的乐谱。读取缓存或执行 prebakeTimeline 计算均推迟到 do ... while 内真正轮到该歌曲时执行。当单曲执行完毕后，执行显式内存回收：music_info.mergedTimeline = null; music_info = null;。这样每次一首歌弹完，庞大的打点时间轴数组引用会立刻被垃圾回收，使得内存能够保持稳定。

### v0.1.11 播放器部分单独拆分到player.js

- main.js 现在只需要负责设置解析、文件检查、曲谱预处理和缓存管理等功能，时间轴播放逻辑完全拆分到了 player.js 中。
- 这样后续将该脚本转移到 Android 端或其他平台时，可以尽可能复用 main.js 中的通用读谱逻辑，而 player.js 的播放逻辑可以定制化。

## 本 Android 项目的开发节点的记录

1. 初始化了项目，编写了 README.md 的规划，将原始 JS 脚本导入到仓库。
2. 要求 Copilot 生成了 AGENTS.md 和 GeneralPlan.md 的初稿。AGENTS 快速介绍项目现况，PLAN 指出后续方向。后续对 GeneralPlan 进行了讨论完善，将其作为总大纲文件。具体的不同需求将再区分到不同的进程中分别去做，不在这一步。
3. 完成 Step1 规划并落地曲谱解析与缓存机制核心：新增 core 模块数据模型、解析/预烘焙/缓存存取与日志标签规则，补充单元测试，并生成 Step1 规格文档；修复 Windows 下资源/源码符号链接导致的 Gradle 快照问题后重跑测试，以测试与构建通过作为质量保障。
   - 在此阶段，将“完成计划后，将一段简短的总结写入开发节点记录”写入了 AGENTS.md。
4. 完成 Step2 运行时播放调度实现：新增运行时播放配置、调度器与触控注入接口，采用 `SystemClock.uptimeMillis` 的延迟-自旋调度，补充开始/暂停/停止/上一首/下一首与 `releaseAllTouches` 控制语义，并落地 `CopilotDocs/step2/plan.md`。
   - 在此阶段，明确了将开发中不同阶段的计划写入 `CopilotDocs/stepX/plan.md` 的规范，计划中需要明确本阶段实现的、留待后续实现的，以避免范围漂移和混乱。
5. 完成 Step3 触控注入与无障碍服务接入：新增无障碍服务、触控注入与坐标映射实现，更新 Manifest 与服务配置，补充坐标映射单元测试，并落地 `CopilotDocs/step3/plan.md`。
6. 完成 Step4 悬浮窗播放控制与前台服务：新增播放状态快照与监听、Compose 悬浮控制面板、`specialUse` 前台服务和临时验收入口；悬浮窗采用顶部对齐坐标映射并限制在琴键安全区域内，仅在非播放状态允许拖动；补充位置映射与播放状态单元测试，并落地 `CopilotDocs/step4/plan.md`。
   - 在此期间，从 Copilot 切换到了 Codex，因此将一些 Copilot 原有的内置技能做了显式的 SKILL 更新，以适配 Codex 的能力。具体地说：克隆了 `agent-customization`。新增 workspace skill `update-agent-instructions`，用于从代码库可验证现状出发，对项目根目录现有 `AGENTS.md` 做最小、保留原有结构的增量更新，并规范一次性发现其他 AI 指令、差异核对和更新后验证流程。
7. 完成 Step5 正式主页面与准备演奏入口：将 `MainActivity` 从 Step4 临时验收入口替换为 Compose 主页面，自动使用排序后的第一首曲谱，提供曲谱管理和播放配置占位页，接入曲谱预加载缓存保存流程，并在缓存、悬浮窗权限和无障碍服务均就绪后启动悬浮窗演奏服务；补充主页面缓存状态与预加载单元测试，并落地 `CopilotDocs/step5/plan.md`。
8. 完成 Step6 曲谱管理页面：新增系统文件选择器导入 JSON、表单化手动创建、曲谱列表展示、重名覆盖确认和删除曲谱同步清理缓存；补充严格曲谱校验入口与曲谱管理单元测试，并落地 `CopilotDocs/step6/plan.md`。
9. 完成 Step7 播放配置界面与会话配置接入：新增播放配置草稿、持久化和应用控制器，覆盖原 `settings.json` 的定时启动、四种播放模式、单曲/队列选择、队列间隔、循环次数、循环间隔和调试模式；主页面改为按已应用配置队列预加载与准备演奏，并补充配置解析、队列规则、持久化和队列缓存单元测试，落地 `CopilotDocs/step7/plan.md`。
10. 完成 Step8 权限引导与 UI 体验打磨：新增权限引导状态模型、悬浮窗/无障碍权限待办、首页启动权限弹窗和准备演奏阻塞原因展示；新增无障碍设置跳转入口，整理首页、曲谱管理页、播放配置页与悬浮窗面板的基础信息层级和按钮布局；补充权限引导 JVM 单元测试，并落地 `CopilotDocs/step8/plan.md`。
11. 完成 README 使用与曲谱说明补充：结合 Android 版当前流程和原 JS 脚本读谱规则，新增“使用说明”和“曲谱编写说明”，覆盖权限准备、曲谱导入/创建、播放配置、预加载、悬浮窗控制、JSON 字段、严格校验和键盘谱读谱规则。
    - bugfix1. 修复横屏游戏模式下悬浮窗因安全区不足而自动关闭的问题：悬浮窗位置不足时改为贴顶约束而不是退出服务，并取消状态栏/刘海对悬浮窗的上边界限制；同时缩小悬浮控制面板、移除面板内错误/日志显示，横屏宽屏时使用紧凑单行按钮和同一行左右状态显示；在开始演奏后，移除外层背景/阴影、将面板设为 50% 半透明。
        - 在此期间，确定了“开发”和“修复”的命名和规划区分：前者采用 `stepX`，版本号更新为 v `x.y+1.0`；后者采用 `bugfixX`，版本号更新为 v `x.y.z+1`。重大更新时，版本号更新为 v `x+1.0.0`。
    - bugfix2. 将“新建曲谱”从曲谱管理页内展开调整为独立页面，迁移原有手动创建表单并清空所有默认填充值；保存成功后返回曲谱管理页并刷新曲谱/播放准备状态，同时将 Android 版本更新为 `v1.0.1`。
12. 完成 Step9 关于页面与应用内更新检查：新增首页右上角三点入口和“关于”页面，展示版本号与可跳转仓库地址；通过 GitHub 最新正式 Release 与本地语义版本比较检查更新，发现新版本时下载 `app-debug.apk` 到应用私有缓存并拉起系统安装器；补充 FileProvider、网络/安装权限、更新缓存清理策略和关于页逻辑单元测试，并落地 `CopilotDocs/step9/plan.md`。
    - bugfix3. 下载/Release asset 名称从写错了的 apk-debug.apk 改正为 app-debug.apk，测试和 step9 文档也同步更新。 “仓库地址”改成关于页里的可点击按钮，点击会用系统浏览器打开仓库 URL。 版本更新为 `v1.1.1`。
    - bugfix4. 对关于页面的 UI 做了调整。版本更新为 `v1.1.2`。
      - 在此阶段，由于 Codex 在工作时表现与预期出现偏差（比如 AGENTS.md 让它用 UTF-8 读文档愣是不用），且每个步骤里的对话中要求多有重复，因此将该部分内容作为 SKILL 进行了显式强化，免得我次次念叨。
    - bugfix5. 将首页右上角“关于”入口从文本三点改为 Compose 绘制的三点图案按钮，保留 48dp 触控目标、点击进入关于页和无障碍说明；版本更新为 `v1.1.3`。
13. 完成 Step10 程序结构优化：将 Step10 计划落地到 `CopilotDocs/step10/plan.md`。把主界面 Compose 页面从 `MainActivity` 拆分到 `main/ui`；将主要用户可见文本迁移到中英文 `strings.xml`；按 core/accessibility/overlay/main 领域集中维护可配置常量；版本更新为 `v1.2.0`。
14. 完成 Step11 界面表现优化：新增 `CopilotDocs/step11/plan.md`；为主 UI 页面切换、消息块和列表选择反馈加入轻量动画；曲谱 JSON 严格校验失败时返回来源、字段和行列位置；播放配置队列模式改为按点击顺序编号的复选列表；保存并应用成功后返回首页并通过 Snackbar 显示“设置已保存”；补充相关 JVM 单元测试，版本更新为 `v1.3.0`。
    - bugfix6. 将曲谱 JSON 严格校验的用户可见错误从字段/行列技术定位改为填空栏目语义提示，例如“曲谱名为空”“拍号格式错误”等；手动新建曲谱时可定位的错误会将对应输入栏目标红，并在出现曲谱错误时自动滚动到错误提示位置；JSON 语法错误仍保留行列位置；版本更新为 `v1.3.1`。
    - bugfix7. 调整主页面切换动画：放弃淡入淡出，改为新页面从右向左进入、旧页面从左向右退出的水平挤压式切换效果；版本更新为 `v1.3.2`。
